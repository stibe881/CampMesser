# Vorlage für den Selbstbetrieb (z. B. Hetzner Webhosting mit Node.js).
# Auf dem Server kopieren:  cp env.hetzner.template .env  &&  chmod 600 .env

# --- Pflicht ---
# Verbindung zur MySQL-Datenbank. Sonderzeichen im Passwort URL-kodieren (@ = %40).
DATABASE_URL=mysql://benutzer:passwort@host.your-database.de:3306/datenbankname

# Zufälliges Geheimnis zum Signieren der Anmelde-Sitzungen.
# Erzeugen mit: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Betriebsmodus - fuer den Livebetrieb immer "production".
NODE_ENV=production

# Port, auf dem der Server lauscht (Vorgabe des Hosters uebernehmen).
PORT=3000

# --- Optional: E-Mail-Versand fuer "Passwort vergessen" ---
# Zugangsdaten eines Postfachs deiner Domain (z. B. Hetzner-Postfach,
# Port 587 mit STARTTLS). Ohne diese Angaben meldet die App beim
# Passwort-Reset, dass die Funktion derzeit nicht verfuegbar ist.
# Mit SMTP ist automatisch auch die E-Mail-Bestaetigung neuer Konten
# aktiv (48-h-Link; unbestaetigte Konten werden nicht eingeschraenkt).
SMTP_HOST=mail.deinedomain.ch
SMTP_PORT=587
SMTP_USER=noreply@deinedomain.ch
SMTP_PASS=
SMTP_FROM=noreply@deinedomain.ch

# Oeffentliche Adresse der App - wird fuer die Links in den Reset-Mails
# verwendet UND bestimmt die Passkey-Domaene (WebAuthn rpID/Origin).
# Fuer die Passkey-Anmeldung MUSS die Angabe exakt zur oeffentlichen
# Domain passen (Livebetrieb: https://campmesser.ch) - sonst verweigern
# Browser das Anlegen und Verwenden von Passkeys.
# Ohne Angabe wird der Host der Anfrage genutzt.
APP_URL=https://deinedomain.ch

# --- Optional: Unwetter-Push fuer gespeicherte Zeltplaetze ---
# Schluesselpaar erzeugen mit: pnpm exec web-push generate-vapid-keys
# Ohne diese Angaben bleibt der Push-Schalter in der App ausgeblendet.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:webmaster@deinedomain.ch
# Geheimnis fuer den Cron-Aufruf von /api/push/check (zufaellige Zeichenkette).
CRON_SECRET=
