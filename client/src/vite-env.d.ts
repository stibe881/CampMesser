/// <reference types="vite/client" />

/** Beim Build eingebetteter Git-Commit-Hash (Fallback «dev»). */
declare const __APP_VERSION__: string;
/** Beim Build eingebetteter Zeitpunkt (ISO-String). */
declare const __APP_BUILT_AT__: string;
